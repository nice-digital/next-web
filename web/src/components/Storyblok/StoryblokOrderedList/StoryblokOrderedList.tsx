import React from "react";

export const StoryblokOrderedList = (blok: any) => {
	console.log("StoryblokOrderedList blok:", blok.listItems);
	return (
		<div>hgwhjqw</div>
		// <ol type="A" start={3}>
		// 	{blok.listItems.map((item: any, index: number) => (
		// 		<li key={index}>
		// 			<a href="#">{item.text}</a>
		// 		</li>
		// 	))}
		// </ol>
	);
};
